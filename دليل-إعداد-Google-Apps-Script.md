# دليل إعداد وتنفيذ Google Apps Script

---

## 1. إنشاء مشروع Google Apps Script جديد

1. اذهب إلى [Google Apps Script](https://script.google.com/) و سجل دخول باستخدام حساب Google الذي تريد أن يكون هو الحساب المرتبط بالتقويم (مثل startups@athareg.com)
2. اضغط على زر **"New Project"** أو "مشروع جديد"
3. سيتم فتح محرر Apps Script تلقائيًا

---

## 2. إضافة الملفات إلى المشروع

### الخطوة 2.1: تحديث ملف المشروع (appsscript.json)
1. في المحرر، من القائمة اليسرى، اضغط على أيقونة **⚙️ Settings** أو "إعدادات"
2. قم بتفعيل الخيار **"Show 'appsscript.json' manifest file in editor"** أو "إظهار ملف appsscript.json في المحرر"
3. الآن، في القائمة اليسرى، ستجد ملف اسمه `appsscript.json`، اضغط عليه و احذف محتواه القديم
4. انسخ المحتوى من الملف `google-apps-script/appsscript.json` في مشروعك و الصقه هنا
5. غير قيمة `CALENDAR_ID` في ملف `Code.gs` إلى معرف التقويم اللي عاوز تستخدمه (لو عاوز تستخدم التقويم الأساسي للحساب، خليها 'primary')

### الخطوة 2.2: إضافة ملف Code.gs
1. في القائمة اليسرى، اضغط على `Code.gs` (اللي موجودة تلقائيًا) واحذف محتواه القديم
2. انسخ المحتوى من الملف `google-apps-script/Code.gs` في مشروعك و الصقه هنا
3. تأكد من أن القيمة `SUPABASE_ANON_KEY` في `Code.gs` صحيحة (تبقى في ملف `src/lib/supabase.js`)

---

## 3. تفعيل الخدمات المتقدمة (Calendar Advanced Service)
1. في شريط الأدوات، اضغط على **Services** أو "الخدمات"
2. في مربع البحث، ابحث عن **"Google Calendar API"** واخترها
3. اضغط على **"Add"** أو "إضافة"
4. تأكد من أن الـ "Identifier" هو `Calendar` (ك اللي مكتوب في الملف `appsscript.json`) واضغط Save أو "حفظ"

---

## 4. اختبار الاتصال
1. في `Code.gs`، ابحث عن الدالة `testSetup`
2. من القائمة اليسرى بجوار "Debug"، اختر `testSetup` من القائمة
3. اضغط على زر **"Run"** أو "تشغيل"
4. سيطلب منك تصريحًا للوصول إلى حسابك Google، اضغط على "Review permissions" أو "مراجعة الصلاحيات" واتبع الخطوات للسماح
5. بعد التأكيد، راجع "Execution log" أو "سجل التنفيذ" للتأكد من أن كل شيء يعمل

---

## 5. نشر المشروع كـ Web App (لإستقبال Webhooks)
1. في الشريط الأيمن أو الأعلى، اضغط على **"Deploy"** أو "نشر" → **"New deployment"** أو "نشر جديد"
2. في القائمة "Select type" أو "اختر النوع"، اختر **"Web app"** أو "تطبيق ويب"
3. املأ الحقول كما يلي:
   - **"New description"** أو "وصف جديد": وصف ما (مثل "Booking Automation System")
   - **"Execute as"** أو "تنفيذ باسم": اختر **"Me (your-email@example.com)"** أو "أنا" (حسابك اللي عامل به تسجيل الدخول)
   - **"Who has access"** أو "الذين لديهم صلاحية الوصول": اختر **"Anyone"** أو "أي شخص"
4. اضغط على **"Deploy"** أو "نشر"
5. سيظهر لك رابط، اضغط على **"Copy"** و احفظه (هذا هو الـ Web App URL اللي بنستخدمه في الخطوات اللي بعدها)

---

## 6. تحديث روابط Web App في مشروعك
1. افتح ملف `src/lib/atherTeamBooking.js`
2. ابحث عن السطر اللي فيه `const webAppUrl = ...` و استبدل الرابط بالرابط اللي نسخته في الخطوة السابقة
3. افتح ملف `src/lib/mentorBooking.js` واعمل نفس الشيء (استبدل الرابط بالرابط اللي نسخته)

---

## 7. إعداد Trigger زمني (اختياري، كبديل للـ Webhook)
لو عاوز الـ Apps Script يتحقق تلقائيًا كل فترة من الحجوزات الجديدة (بدلاً من الـ Webhook)، نقدر نعمل Trigger زمني:

1. في Apps Script، اضغط على **"Triggers"** أو "المشغلات" من القائمة اليسرى
2. اضغط على **"Add Trigger"** أو "إضافة مشغل"
3. املأ الحقول:
   - **"Choose which function to run"**: اختر `pollForNewBookings`
   - **"Choose which deployment to run"**: اختر "Head"
   - **"Select event source"**: اختر "Time-driven" أو "مقوم بالوقت"
   - **"Select type of time based trigger"**: اختر "Minute timer" أو "مؤقت دقائق"
   - **"Select minute interval"**: اختر الفترة اللي عاوزها (مثل "Every 5 minutes" أو "كل 5 دقائق")
4. اضغط على **"Save"** أو "حفظ"

---

## 8. اختبار النظام كامل!
الآن، ابدأ تجربة الحجز من المنصة (Selection Process):
1. اختر موعدًا
2. اكمل الحجز
3. بعد ثواني، راجع تقويم Google الخاص بك، هتجيب حدث جديد مع رابط Google Meet!
4. راح يصلكم الإيميلات تلقائيًا للجميع (المرشح، المستشار، الأدمن، إلخ)

---

## التأكد من النتائج
- [ ] الحجز محفوظ في Supabase مع `meet_link`
- [ ] الحدث موجود في التقويم مع رابط Meet صحيح
- [ ] الضيوف (Attendees) مضافين للحدث
- [ ] الإيميلات وصلت للمرشح وللجميع
